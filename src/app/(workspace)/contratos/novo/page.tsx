import {ContractForm} from '@/components/contracts/contract-form';import {contractOptions} from '@/modules/contracts/repository';import {getRecord} from '@/modules/crm/repository';import {getOpportunity} from '@/modules/commercial/repository';import {pageActor} from '@/server/session';
export default async function Page({searchParams}:{searchParams:Promise<{client_id?:string;opportunity_id?:string}>}){const actor=await pageActor(),query=await searchParams,options=await contractOptions(actor);let client=null,opportunity=null;if(query.opportunity_id){const found=await getOpportunity(actor,query.opportunity_id);opportunity={id:found.id,name:found.title};if(found.customer_id){const customer=await getRecord(actor,found.customer_id);client={id:customer.id,name:customer.name};}}else if(query.client_id){const customer=await getRecord(actor,query.client_id);client={id:customer.id,name:customer.name};}return <><div className="page-heading"><div><span className="eyebrow green">VENDAS E FINANCEIRO</span><h1>Novo contrato</h1><p>Cadastre os itens e defina o plano de recebimento.</p></div></div><section className="card"><ContractForm options={options} userId={actor.userId} initialClient={client} initialOpportunity={opportunity}/></section></>;}
