import {ContractForm} from '@/components/contracts/contract-form';import {contractOptions,getContractDetail} from '@/modules/contracts/repository';import {pageActor} from '@/server/session';
export default async function Page({params}:{params:Promise<{id:string}>}){const actor=await pageActor(),{id}=await params;const [detail,options]=await Promise.all([getContractDetail(actor,id),contractOptions(actor)]);return <><div className="page-heading"><div><span className="eyebrow green">{detail.contract.contract_number}</span><h1>Editar contrato</h1></div></div><section className="card"><ContractForm options={options} userId={actor.userId} detail={detail}/></section></>;}
